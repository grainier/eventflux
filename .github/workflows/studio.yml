# Copyright 2025-2026 EventFlux.io
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: EventFlux Studio

on:
  push:
    branches: [main]
    paths:
      - 'studio/**'
      - 'src/bin/generate_schema.rs'
      - '.github/workflows/studio.yml'
  pull_request:
    branches: [main]
    paths:
      - 'studio/**'
      - 'src/bin/generate_schema.rs'
      - '.github/workflows/studio.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to VS Code Marketplace'
        required: false
        type: boolean
        default: false

env:
  CARGO_TERM_COLOR: always

jobs:
  generate-schema:
    name: Generate Schema
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-schema-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-schema-

      - name: Generate EventFlux Schema
        run: cargo run --bin generate_schema

      - name: Upload schema artifact
        uses: actions/upload-artifact@v7
        with:
          name: eventflux-schema
          path: studio/webview/src/schemas/eventflux-schema.json
          retention-days: 1

  build:
    name: Build Extension
    runs-on: ubuntu-latest
    needs: generate-schema
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download schema artifact
        uses: actions/download-artifact@v8
        with:
          name: eventflux-schema
          path: studio/webview/src/schemas/

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            studio/package-lock.json
            studio/webview/package-lock.json

      - name: Install extension dependencies
        working-directory: studio
        run: npm ci

      - name: Install webview dependencies
        working-directory: studio/webview
        run: npm ci

      - name: Build extension
        working-directory: studio
        run: npm run build

      - name: Package extension
        working-directory: studio
        run: |
          npm install -g @vscode/vsce
          vsce package --no-dependencies

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v7
        with:
          name: eventflux-studio-vsix
          path: studio/*.vsix
          retention-days: 30

  publish:
    name: Publish Extension
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
    steps:
      - name: Download VSIX artifact
        uses: actions/download-artifact@v8
        with:
          name: eventflux-studio-vsix
          path: ./

      - name: Publish to VS Code Marketplace
        run: |
          npm install -g @vscode/vsce
          vsce publish --packagePath *.vsix
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Upload VSIX to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: '*.vsix'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
